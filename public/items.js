/*const items=require('../db').Product
  
route.post('/',(req,res)=>{
    let item=req.body.id
       /* name:req.body.name,
        price:req.body.price
        call(item)
   })
   function call(data){
     let allitem=items.findall()(
       {where:{id:data}}
     )
     route.get('/',(req,res)=>{
      res.send(allitem)
  })
   }*/